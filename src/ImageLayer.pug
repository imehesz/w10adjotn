script.
    import { createEventDispatcher } from "svelte"

    export let idx
    export let imageUrl
    export let isSelected
    export let weight
       
    const publish = createEventDispatcher()

    const onSelect = () => publish("setSelected", idx)

    let blah = "BLAH"

div.image-layer
    p {idx} image layer {weight} {imageUrl} {isSelected}

    button("on:click"={onSelect}) SELECT
